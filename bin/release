#!/bin/sh

cat << EOF
---
addons:
config_vars:
  PATH: /app/local/bin:/app/.pyenv/bin:/app/.pyenv/shims:/usr/local/bin:/usr/bin:/bin
  PERL5LIB: /app/local/lib/perl5
default_process_types:
  web: ls -al /app/.plenv/shims && carton exec starman --preload-app --port \$PORT
EOF
